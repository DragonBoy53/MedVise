{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // This works because index.js is in the same folder
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "index.js"
    }
  ]
}
